import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Turbopack empty config is no longer strictly needed since we removed custom webpack plugins,
  // but it's safe to leave or Next.js just uses defaults.
};

export default nextConfig;
